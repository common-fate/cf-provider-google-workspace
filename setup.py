from setuptools import setup, find_packages

setup(
    name="cf-provider-google-groups",
    version="",
    packages=find_packages(),
    install_requires=[
        # list of package dependencies
        "commonfate-provider"
    ],
    # other metadata such as author, license, etc.
)
